export const metadata = {
    title: 'Sign In',
    description: 'Sign in to your Valerie Anne Barber Gallery account.',
}

export default function LoginLayout({
    children,
}: Readonly<{
    children: React.ReactNode
}>) {
    return children
}
