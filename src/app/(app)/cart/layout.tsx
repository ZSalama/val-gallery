export const metadata = {
    title: 'Shopping Cart',
    description: 'Review your Valerie Anne Barber Gallery cart.',
}

export default function CartLayout({
    children,
}: Readonly<{
    children: React.ReactNode
}>) {
    return children
}
